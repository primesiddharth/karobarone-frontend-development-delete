"use client";
import Link from "next/link";
import { Search, ChevronRight, PackageOpen } from "lucide-react";
import { CommerceShell } from "@/components/commerce/CommerceShell";
import { orders, money } from "@/components/commerce/data";
import { useState } from "react";
const statusClass=(s:string)=>s==="Delivered"?"bg-green-50 text-green-700":s==="Cancelled"?"bg-red-50 text-red-700":s==="Shipped"?"bg-blue-50 text-blue-700":"bg-amber-50 text-amber-700";
export default function OrdersPage(){const [query,setQuery]=useState(""); const filtered=orders.filter(o=>o.id.toLowerCase().includes(query.toLowerCase())||o.status.toLowerCase().includes(query.toLowerCase())); return <CommerceShell title="Orders" eyebrow="Orders & Payments"><div className="mx-auto max-w-7xl px-6"><div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-gray-500">Track and manage all your orders.</p><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-gray-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search order or status" className="w-full rounded-lg border border-gray-200 py-2.5 pl-9 pr-3 text-sm outline-none focus:border-[#5b4ef9]"/></div></div><div className="overflow-hidden rounded-2xl border border-gray-200 bg-white"><div className="hidden overflow-x-auto md:block"><table className="w-full"><thead className="bg-gray-50"><tr>{["Order","Date","Items","Amount","Status","Action"].map(h=><th key={h} className="border-b border-gray-200 px-5 py-4 text-left text-xs font-semibold uppercase tracking-wider text-gray-500">{h}</th>)}</tr></thead><tbody>{filtered.map(o=><tr key={o.id} className="hover:bg-gray-50/70"><td className="px-5 py-4 font-medium">{o.id}</td><td className="px-5 py-4 text-sm text-gray-600">{o.date}</td><td className="px-5 py-4 text-sm text-gray-600">{o.items}</td><td className="px-5 py-4 text-sm font-medium">{money(o.amount)}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${statusClass(o.status)}`}>{o.status}</span></td><td className="px-5 py-4"><Link href="#" className="inline-flex items-center gap-1 text-sm font-medium text-[#5b4ef9]">View <ChevronRight className="h-4 w-4"/></Link></td></tr>)}</tbody></table></div><div className="divide-y divide-gray-100 md:hidden">{filtered.map(o=><div key={o.id} className="p-5"><div className="flex justify-between gap-3"><div><p className="font-medium">{o.id}</p><p className="mt-1 text-sm text-gray-500">{o.date} · {o.items} items</p></div><span className={`h-fit rounded-full px-2.5 py-1 text-xs font-medium ${statusClass(o.status)}`}>{o.status}</span></div><div className="mt-4 flex items-center justify-between"><span className="font-semibold">{money(o.amount)}</span><Link href="#" className="text-sm font-medium text-[#5b4ef9]">View order</Link></div></div>)}</div>{!filtered.length&&<div className="px-6 py-16 text-center"><PackageOpen className="mx-auto h-8 w-8 text-gray-300"/><p className="mt-4 font-medium">No orders found</p></div>}</div></div></CommerceShell>}
